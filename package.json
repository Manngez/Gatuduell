{
  "name": "umea-gatduell",
  "version": "1.0.0",
  "private": true,
  "description": "Fristående Gatduell för Umeås gatunät",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check engine.js && node --check app.js"
  }
}
